
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all collections for a single-user app model.
    // This is suitable for rapid prototyping but should be secured before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
